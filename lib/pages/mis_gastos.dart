import 'package:flutter/material.dart';

class MisGastos extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Container(
        child: new Center(
            child: new Icon(Icons.account_balance, size: 150.0, color: Colors.brown)
        )
    );
  }
}